#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../micro-core/bin/micro-serve" "$@"
  ret=$?
else 
  node  "$basedir/../micro-core/bin/micro-serve" "$@"
  ret=$?
fi
exit $ret
